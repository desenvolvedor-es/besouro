<header>
	<!--img src="images/besouro.gif" alt="{{title}} Logo" title="{{title}}" style="float: left;"-->
	<h1>{{title}}</h1>
	<p>Welcome to {{title}}</p>
	{% block header %}
		{% include 'partials/search-bar.twig' %}
	{% endblock %}
	{% block menu %}
		{% include 'partials/menu.twig' with { menu: menu } %}
	{% endblock %}
</header>